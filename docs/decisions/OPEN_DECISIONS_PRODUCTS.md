# سجل القرارات المفتوحة — فرع المنتجات

**قاعدة العمل:** لا يُغلق فرع المنتجات ولا يُقترح الانتقال إلى فرع جديد ما دام هذا السجل يحتوي قرارًا بحالة `مفتوح`، إلا إذا طلب العميل تأجيله صراحةً وحدد سبب التأجيل.

| المعرّف | القرار أو السؤال | الحالة | القاعدة المعتمدة | المصدر |
|---|---|---|---|---|
| PROD-OPEN-001 | عند وصول رسالة موظف: «نفد البيج» لمنتج لديه قياسات، هل يعدّ البوت كل تركيبات لون البيج نافدة، أم يطلب تحديد القياس أولًا؟ | مغلق | الصورة الأصلية تحدد المنتج ثم اللون. في المنتج الذي يحمل قياسات، يجب أن يذكر الموظف القياس في النص، مثل: صورة البيج + `M نفد`. عند غياب القياس لا ينفذ البوت تعديل مخزون؛ يطلب القياس. المنتج ذو اللون فقط ينفذ من الصورة ورسالة النفاد أو زيادة الكمية بعد تحقق المطابقة. | شرح العميل في هذه الجلسة، مؤيد بقرار المحادثة الأصلية عن المطابقة داخل صور المنتج. |
| PROD-OPEN-002 | بينما Microsoft Graph يعيد `serviceReadOnly` ويمنع اختبار الاتصال الحي، هل نبني قارئ/مستورد الكتالوج واختباراته المعزولة الآن ثم نربطه عند عودة الخدمة، أم نؤجل البناء إلى أن تعمل الخدمة؟ | مغلق | وافق العميل على تجربة واحدة عبر OAuth و`Files.Read`: يختار بنفسه جذر `Catalog`، ولا تحفظ المنصة كلمة المرور ولا تستورد منتجًا قبل مراجعة المسودة. يقيّد النظام استخدامه وظيفيًا بالمجلد المختار، مع توثيق أن الحساب الشخصي لا يتيح قيد Graph دائمًا على مجلد واحد فقط. | موافقة العميل في هذه الجلسة بعد عرض حدود الصلاحية. |
| PROD-OPEN-003 | نجح OAuth وقراءة مجلدات جذر OneDrive، لكن لم يظهر مجلد باسم `Catalog`. هل ننشئ/نعيد تسمية الجذر إلى `Catalog` في OneDrive، أم نعتمد اسمًا أو مسارًا قائمًا يختاره العميل صراحة؟ | مغلق | أنشأ العميل أو أعاد تسمية مجلد الجذر إلى `Catalog`، ثم اختارته المنصة وحفظته مرجعًا فقط. | تأكيد العميل والاختبار الحي في شاشة المنتجات. |
| PROD-OPEN-004 | كشف اختبار Catalog أن `HJB-TEST-001` يقع مباشرة تحت `Catalog`، بينما الشجرة المعتمدة تتطلب `Catalog/المجموعة/كود المنتج`. هل يعدل العميل مجلد الاختبار إلى شجرة المجموعة المعتمدة، أم تريد المنصة دعم منتجات مباشرة تحت الجذر كاستثناء دائم؟ | مغلق | نُقل المنتج إلى `Catalog/حجابات الاميرة/HJB-TEST-001/`، ونجحت المنصة في قراءة المجموعة والكود و`product.txt` والصورتين. | اختبار حي ناجح في شاشة المنتجات. |
| PROD-OPEN-005 | ظهرت ثلاث نقاط `...` في بداية قيم الاسم والسعر والوصف داخل `product.txt`، مثل `SELLING_PRICE_IQD: ...8000`. هل تعدّ هذه النقاط جزءًا مقصودًا من القيم أم بقايا من قالب المثال يجب أن تمنع المسودة حتى حذفها؟ | مغلق | `...` بقايا من المثال ويجب حذفها من قيم الملف، ولا ينشئ النظام مسودة عندما تبدأ القيم بها. يبقى سطر `SIZES` موجودًا دائمًا: `SIZES:` عند غياب القياسات، أو قائمة مفصولة بفواصل عند وجودها مثل `SIZES: Medium, Large`. | قرار العميل في هذه الجلسة. |
| PROD-OPEN-006 | اقترح تحليل مرئيات `HJB-TEST-001` لونين: عنابي وزيتي، كل منهما مرتبط بصورة مختلفة. هل تعتمد هذين اللونين لإنشاء متغيرين لمنتج المسودة؟ | مفتوح | اعتماد أو تصحيح أسماء الألوان قبل إنشاء أي متغير أو كمية أو وسائط. | يمنع إنشاء متغيرات اللون ومخزونها. |

### نتيجة اختبار المنتج الحي — 2026-08-23

نجحت قراءة مجلد `HJB-TEST-001` داخل المرجع `Catalog` بصفة معاينة فقط. أثبتت النتيجة وجود **صورتين** وعدم وجود ملف `product.txt`، كما أثبتت أن المجلد موجود مباشرة تحت `Catalog`. لم يُنشأ صف في المنتجات، ولم تُنقل صورة أو وسائط، ولم يتغير المخزون. هذه النتيجة تؤكد أن مسار القراءة يعمل، لكنها لا تحقق قبول الاستيراد بسبب مخالفة الشجرة ونقص ملف البيانات.

### نتيجة إعادة الاختبار من الشجرة المعتمدة — 2026-08-23

بعد نقل المنتج إلى مجموعة `حجابات الاميرة` وإضافة `product.txt`، نجحت المنصة في معاينة: المجموعة، الكود `HJB-TEST-001`، ملف البيانات، وصورتين. بعد حذف `...` وإبقاء `SIZES:` فارغًا، أعادت المعاينة إظهار الاسم `حجاب اميرة كيبور` وسعر البيع `8000` والوصف بلا التباس. ما زالت المعاينة قراءة فقط؛ لم يُنشأ منتج أو متغير أو وسائط، وتبقى خطوة إنشاء المسودة مرهونة بموافقة مستقلة من العميل.

### نتيجة إنشاء المسودة الحية — 2026-08-23

بعد موافقة العميل الصريحة، أنشأت المنصة مسودة المنتج `HJB-TEST-001` بالحالة `draft`، والاسم `حجاب اميرة كيبور`، والمجموعة `حجابات الاميرة`، وسعر البيع `8000.00`. أنشئ معها سجل إدخال من OneDrive بالحالة `needs_review`. تحققنا من قاعدة البيانات أن عدد المتغيرات **0** وعدد الوسائط **0**؛ لذلك لم يُنشأ لون أو مخزون أو قياس أو وسائط ولم ينشر المنتج للعميل.

### نتيجة اقتراح ألوان الصور — 2026-08-23

بعد تجديد رمز Catalog تلقائيًا، حلل النظام مصغرات OneDrive المؤقتة للصورتين من دون تخزينهما أو إنشاء بيانات تشغيلية. اقترح **عنابي** للصورة `20260430_144109875_iOS.png` و**زيتي** للصورة `20260527_152004437_iOS.jpg`، بثقة 100٪ لكل منهما. هذه اقتراحات بشرية للمراجعة وليست ألوانًا معتمدة في قاعدة البيانات؛ لا يوجد حتى الآن متغير أو مخزون أو وسيط مرتبط بهما. لا تعرض الواجهة الصورة الكاملة بعد، وتبقى إضافة معاينة القراءة فقط للصور كمتابعة منفصلة.

## نتيجة مراجعة المصدر

راجعت المقطع الكامل للرسائل 139–169 من المحادثة الأصلية، ثم بحثت في المحادثة المرجعية الكاملة عن «قياس»، «مقاس»، «الأحجام»، و`Large`. يثبت المصدر أن البوت يقرأ الكود، ثم يطابق الصورة داخل صور المنتج لتحديد اللون وينفذ عند الثقة العالية أو يسأل عند غموض اللون. وقد حسم العميل في هذه الجلسة الحد المتبقي: الصورة لا تحدد القياس، لذلك يذكر الموظف القياس نصًا عند تعديل تركيبة لون وقياس، ويطلب البوت هذا القياس إذا لم يرد في الرسالة.

## طريقة التعامل مع السجل

1. كل سؤال أو اقتراح لم يتلق جوابًا صريحًا يُضاف هنا فورًا بحالة `مفتوح`.
2. إذا انتقل النقاش إلى نقطة أخرى، يبقى السؤال مرئيًا ولا يتحول إلى قرار ضمني.
3. عند الإجابة، تسجل القاعدة المعتمدة، ويتحول إلى `مغلق` مع رابط للقرار أو العقد الذي حُدّث.
4. قبل اقتراح الانتقال، تعرض المحادثة عدد القرارات المفتوحة وما يمنع إغلاق الفرع.

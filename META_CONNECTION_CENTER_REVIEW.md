# مراجعة مركز اتصال Meta الموحد

**التاريخ:** 2026-09-02
**النطاق:** إعداد تطبيق Meta من داخل المنصة، OAuth موحد، الأصول المتعددة، القدرات الست، الإبطال وإصلاح الصلاحيات، والتوافق مع القنوات الحالية.

## ما تم تنفيذه

أصبحت صفحة `/meta-connections` تبدأ بزر **«تسجيل الدخول وربط Meta مرة واحدة»** بدلاً من ستة أزرار منفصلة. ينتج التفويض اتصالاً أساسياً واحداً لكل متجر تحت الغرض `unified`، ويحفظ رمز الوصول مرة واحدة بصورة مشفرة، ثم يسقط الصلاحيات الممنوحة على ست قدرات تشغيلية مستقلة: الرسائل والقنوات، المحتوى والتفاعل، قراءة الإعلانات، Lead Ads، Meta Catalog، والقياس والتحويلات.

أضيفت صفحة `/settings/meta-app` لمدير المنصة. تسمح بإدخال App ID وApp Secret وBusiness Login Configuration ID وWhatsApp Embedded Signup Configuration ID واختيار Graph API Version، كما تعرض روابط OAuth وWebhook المنشورة. لا يعاد App Secret أو Verify Token في الاستعلامات؛ يُشفّر كلاهما بـAES-256-GCM، ويظهر Verify Token الجديد مرة واحدة فقط عند التوليد أو التدوير.

تدعم واجهة الأصول البحث والتصفية وإظهار المحدد فقط، وتحديد أكثر من صفحة أو حساب إعلاني، والتحديد والإلغاء الجماعي. كل تبديل يستدعي الخادم ويحفظ `isSelected` ويحدّث حساب القناة والقدرات؛ لم تعد عناصر العرض شكلية. ويمكن تعطيل قدرة منفردة من دون إبطال الاتصال العام، أو بدء إصلاح صلاحياتها عبر رحلة Meta الموحدة، أو إبطال الاتصال كله مع حذف الرمز المشفر وتعطيل أصوله وقدراته.

## الترحيل والتوافق

أضيف الترحيل `0042_chemical_madame_masque.sql` من دون حذف سجلات أو أصول قديمة. يحتفظ النظام باتصالات الأغراض الستة القديمة مؤقتاً حتى نجاح التفويض الموحد، وينقل الاختيارات المتطابقة إلى الاتصال الجديد. بعد وجود سجل موحد، لا يرجع الإرسال أو تنزيل الوسائط إلى رمز قديم إذا أصبح الموحد مبطلاً أو فاشلاً.

تستخدم عمليات OAuth وWebhook والإرسال وتنزيل الوسائط إعداد التطبيق المخزن داخل المنصة. تبقى متغيرات البيئة مسار bootstrap انتقالي فقط إلى أن يحفظ مدير المنصة الإعداد المشفر من الواجهة.

| التحقق | النتيجة |
|---|---|
| TypeScript | `pnpm check` ناجح |
| الاختبارات الكاملة | 58 ملفاً ناجحاً، ملف حي واحد متخطى؛ 126 اختباراً ناجحاً و2 متخطّيان |
| اختبارات Meta الموجهة | 8 اختبارات ناجحة للاتصال والتشفير والأصول والقدرات والترحيل والإبطال |
| تكامل القنوات | اختبارات Webhook والوسائط والإرسال اليدوي ناجحة |
| التشفير | أسرار التطبيق والرموز لا تظهر خاماً، وفكّها يفشل عند تبديل سياق التشفير |
| العزل | لا يمكن اختيار أصل أو قدرة من متجر آخر |
| الأصول المتعددة | اختيار صفحتين وحساب إعلاني معاً ناجح، وتعطيل القدرة يحفظ حالتها |
| الترحيل | الاختيارات القديمة المتطابقة تنتقل، والاتصال القديم لا يُحذف قبل نجاح الموحد |
| الإبطال | لا يسمح بالرجوع إلى رمز الرسائل القديم بعد وجود اتصال موحد مبطل |
| الواجهة | مراجعة سطح المكتب `1440×1000` والهاتف `390×844` ناجحة لصفحتي الإعداد والربط |

## حدود التحقق الحالي

لم يُنفذ تفويض OAuth حي جديد أثناء هذه الدفعة حتى لا تُغيّر حسابات Meta أو الأصول دون إجراء صريح من مدير المتجر. الاختبارات تثبت عمل العقود وقاعدة البيانات والتشفير والواجهة؛ أما إثبات القنوات الثلاث مع الحساب الحقيقي فيبدأ بعد حفظ الإعداد داخل المنصة والضغط على زر الربط الموحد مرة واحدة.

لا يؤدي الربط وحده إلى تشغيل رد البوت أو النشر أو الإنفاق الإعلاني. تظل هذه العمليات تحت مفاتيح القدرات والموافقات والحواجز المالية والأمنية المعتمدة سابقاً.

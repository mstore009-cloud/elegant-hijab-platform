# مراجعة Meta Connection Center

**التاريخ:** 2026-08-28  
**النطاق:** OAuth متعدد الأغراض، الرموز المشفرة، اكتشاف الأصول، اختيار الأصل لكل متجر، الإبطال، ودعم Messenger في حسابات القنوات.

## ما تم تنفيذه

أضيفت جداول `meta_oauth_states` و`meta_connections` و`meta_assets` عبر الترحيلين `0036` و`0037`. تفصل الاتصالات بحسب الغرض: الرسائل، المحتوى، قراءة الإعلانات، Lead Ads، Catalog، والقياس. لا يظهر access token في tRPC أو الواجهة أو سجل التدقيق؛ يُشفّر بـAES-256-GCM مع سياق يربطه بالمتجر والغرض، بينما يُشفّر رمز الصفحة بسياق المتجر ومعرف الأصل.

يسمح `/meta-connections` ببدء Facebook Login for Business، ومراجعة حالة كل غرض، وتحديث الأصول، واختيار Page أو Instagram أو WhatsApp Phone أو أصل آخر، وإبطال الاتصال مع حذف الرمز المشفر محلياً. اختيار أصل رسائل يضع القناة المقابلة في حالة `testing` ولا يفعّل الإرسال.

| التحقق | النتيجة |
|---|---|
| TypeScript | `pnpm check` ناجح |
| الاختبارات المركزة | ملفان، 10 اختبارات ناجحة |
| التشفير | الرمز لا يظهر خاماً، وفكّه يفشل عند تبديل سياق المتجر |
| OAuth state | صالح لعشر دقائق ويُستهلك مرة واحدة |
| العزل | لا يمكن اختيار أصل من اتصال متجر آخر |
| الإبطال | يحذف رموز الاتصال والأصل المشفرة ويزيل الاختيار |
| الواجهة | مراجعة سطح المكتب `1280×720` والهاتف `375×812` ناجحة |

## حدود المرحلة

لا توجد أسرار Meta فعلية في البيئة بعد، ولذلك تعرض الصفحة «ينتظر إعداد تطبيق Meta» وتبقى أزرار الربط غير مفعلة. لم يحدث OAuth حي أو اكتشاف أصول حقيقي أو إرسال رسالة أو نشر محتوى أو إنفاق. هذه الوظائف تتطلب تطبيق Meta وحسابات المستخدم الرسمية وApp Review المناسب.


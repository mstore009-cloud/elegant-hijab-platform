# متابعة جلسة إصلاح تنقل مركز البوت

- [x] حصر صفحات ومكونات مركز البوت وأزرار المعرفة والتعلم.
- [x] إعادة إنتاج مشكلة النقر دون Refresh وتسجيل الخطأ.
- [x] تحديد سبب الحالة أو الراوتر أو التداخل في الأحداث.
- [x] تطبيق إصلاح يحافظ على الرابط والحالة والتنقل المباشر.
- [x] تشغيل TypeScript والبناء والاختبارات.
- [x] اختبار جميع أقسام مركز البوت على سطح المكتب والهاتف.
- [x] حفظ checkpoint بعد التحقق.

## الأقسام المتأثرة

المعرفة، مساعد التعليم، السلوك واللهجة، إجراءات البيع، مصادر التدريب، المسودات والمراجعة، جودة الردود.

## تفاصيل الإصلاح البرمجي
- السبب الجذري: في مكتبة wouter الإصدار 3، تُرجع `useLocation()` فقط مسار `pathname` بدون `search` (query string). كانت الشيفرة القديمة تقرأ التبويب عبر `location.includes("?")` التي كانت دائمًا false، وتعتمد كبديل على `window.location.search` الثابتة غير التفاعلية التي لا يعاد تصييرها عند تغيير الرابط إلا بعد عمل Refresh.
- الحل: استخدام خطاف `useSearch()` الرسمي من wouter داخل `CustomerBotLearning.tsx`، مما جعل المكون يتفاعل فورياً عند كل نقرة لتحديث `activeTab` وعرض محتوى القسم المطلوب مباشرة وتحديث شريط العناوين وحالة التبويب النشط.

# مراجعة الإرسال اليدوي عبر Meta

**التاريخ:** 2026-08-28  
**الحالة:** البنية الداخلية جاهزة ومختبرة بمحاكٍ؛ الاختبار الحي للقنوات الثلاث معلق حتى تفويض حسابات Meta الرسمية.

## ما تم تنفيذه

أضيف سجل `meta_outbound_messages` عبر الترحيل `0041_opposite_screwball.sql`. تمر كل محاولة يدوية من Inbox بمفتاح idempotency ومتجر ومحادثة وحساب قناة، ثم تُسجل كـqueued/sending/sent/failed. لا تُنشأ رسالة Inbox صادرة ناجحة إلا بعد أن يعيد المزود معرف رسالة خارجي.

تقرأ الخدمة الرمز المشفر من أصل Meta المختار داخل المتجر ولا تعيده إلى الواجهة أو سجل التدقيق. يستخدم Messenger صفحة Facebook وPage access token، ويستخدم Instagram المرتبط بالصفحة Page ID ورمز الصفحة، ويستخدم WhatsApp Phone Number ID مع رمز اتصال الرسائل. يبقى Instagram Login المستقل مساراً منفصلاً غير مفعّل.

## الحواجز

| الحاجز | السلوك |
|---|---|
| عزل المتجر | تُرفض المحادثة أو القناة أو الأصل من متجر آخر قبل استدعاء المزود |
| حالة القناة | لا يسمح بالإرسال إلا لحالة `testing` أو `connected` |
| نافذة الرسائل | تُرفض الرسالة الحرة إذا لم توجد رسالة واردة خلال آخر 24 ساعة |
| حدود النص | Instagram حتى 1000 بايت، Messenger حتى 2000 بايت، والحد العام 4000 حرف |
| التكرار | نفس idempotency key يعيد نتيجة المحاولة الأولى ولا يرسل مرة ثانية |
| الفشل | يسجل الخطأ في outbox وحساب القناة ولا ينشئ رسالة صادرة ناجحة |
| الرد الآلي | غير مفعّل؛ المسار الحالي `manual` فقط وبصلاحية `inbox.reply` |

## التحقق

نجح `pnpm check` و**58 ملف اختبار و122 اختباراً**. يغطي الاختبار الجديد القنوات الثلاث، وفك الرمز الصحيح، ومنع التكرار، وفشل المزود، ونافذة 24 ساعة، وعزل المتاجر. تمت مراجعة `/inbox` على سطح المكتب `1280×720` والهاتف `375×812`؛ يظهر الفرق بوضوح بين السجل الداخلي والإرسال اليدوي الجاهز للقناة.

## ما لم يُختبر بعد

لم تُفوض حسابات WhatsApp وMessenger وInstagram الحقيقية بعد، لذلك لم تُرسل رسالة خارجية ولم تُثبت حالة delivered/read من Meta. يبقى بند E3 مفتوحاً حتى إدخال إعداد تطبيق Meta عبر المسار الآمن ثم اختبار رسالة وصورة وحالة تسليم في كل قناة.


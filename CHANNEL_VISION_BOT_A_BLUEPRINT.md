# مخطط تنفيذ Channel-Vision-Bot-A

**الحالة المستهدفة قبل تفويض Meta:** وحدة كاملة قابلة للاختبار بمحاكاة webhooks والصور، تعرض القنوات كغير متصلة وتمنع الإرسال الخارجي.  
**الحالة المستهدفة بعد التفويض:** استقبال محدود للرسائل والصور من حساب WhatsApp Business وحساب Instagram Professional المعتمدين، مع إنشاء مسودة داخل Inbox ومراجعة بشرية.

## نموذج البيانات

| الجدول | الغرض | القيود الرئيسية |
|---|---|---|
| `channel_accounts` | ربط متجر بقناة ومعرف الحساب الخارجي وحالته | `storeId` إلزامي؛ معرف المزود فريد عالمياً لكل قناة؛ لا أسرار أو tokens |
| `channel_webhook_events` | دليل استقبال ومعالجة كل رسالة خارجية | مفتاح حدث فريد لكل حساب؛ hash للحمولة؛ حالات received/processed/ignored/failed؛ لا payload خام |
| `inbox_message_media` | مرفقات الرسالة المخزنة | `storeId` + `messageId` + `channelAccountId`؛ مفتاح S3؛ نوع وحجم وSHA-256؛ معرف وسائط المزود فريد داخل الحساب |
| `customer_bot_image_analyses` | نتيجة رؤية غير تنفيذية لكل صورة | سجل متجر وصورة ونموذج وثقة ووصف بصري وحالة؛ الصورة الواحدة لها آخر تحليل واحد في الدفعة |
| `customer_bot_image_matches` | منتجات مرشحة من متجر الصورة فقط | تحليل + منتج + صورة مرجعية + ثقة وترتيب وسبب؛ لا سعر تكلفة أو هامش |

## عقود الاستقبال

ينشأ endpoint عام واحد `GET/POST /api/webhooks/meta`. GET يقارن verify token ويرجع challenge فقط عند التطابق. POST يقرأ raw bytes قبل JSON، يتحقق بتوقيت ثابت من HMAC-SHA256، ثم يطبع أحداث WhatsApp وInstagram إلى صيغة داخلية واحدة. لا يعتمد `storeId` من payload؛ يحدد المتجر حصراً من `channel_accounts.providerAccountId`.

لكل رسالة مفتاح خارجي `wamid` أو `mid`. تُحجز في `channel_webhook_events` قبل إنشاء المحادثة أو الرسالة. إذا سبق المفتاح ترجع المعالجة `ignored_duplicate` دون إنشاء أثر ثانٍ. تُطابق المحادثة بواسطة `(storeId, channel, senderScopedId)` وتفتح من جديد عند وصول رسالة جديدة.

## مسار الصورة

رسالة الصورة تُسجل أولاً بنص وصفي آمن حتى لو تعذر التنزيل. ينزل عامل المعالجة الصورة بمهلة محددة وحد أقصى 8MB عام، مع سقف 5MB لصورة WhatsApp و8MB لصورة Instagram، ويتحقق من JPEG/PNG ومن SHA-256. ترفع bytes إلى S3 تحت `stores/{storeId}/inbox/{conversationId}/`، ثم يحذف الاعتماد على رابط Meta المؤقت.

تستخدم خدمة الرؤية `gemini-3-flash-preview` إذا بقي متاحاً في الكتالوج الحي، وإلا أول نموذج Gemini متاح. ترجع JSON منظماً: نوع القطعة، اللون الرئيسي، الألوان الثانوية، النقش، النص الظاهر، وصف مختصر، وهل الصورة صالحة لمطابقة منتج. تُقارن الصورة بحد أقصى آمن من المنتجات النشطة وصور تشغيلية تمثل كل منتج، وتخرج أفضل المرشحين مع confidence وسبب. لا يُذكر للزبون تطابق مؤكد إذا لم يتجاوز حد الثقة؛ تتحول الحالة للمراجعة البشرية.

## دمج Bot-H1/H2

تضاف إلى `BotFacts` مصفوفة `imageAnalyses` خاصة برسالة المصدر فقط، تحتوي الوصف والمرشحين الآمنين: productCode وname وsellingPrice والألوان المتاحة. لا تمرر storage key أو رابطاً دائماً أو تكلفة أو هامشاً. يبقى `generateDraft` بطلب موظف في هذه الدفعة؛ إذا لم يكتمل التحليل أو فشل أو كانت الثقة منخفضة، ينتج `human_handoff` أو مسودة تسأل سؤالاً توضيحياً وفق الحاجز.

## الصلاحيات والواجهة

تستخدم إدارة القنوات صلاحية `bot.manage`، وقراءة الوسائط والتحليل صلاحية `inbox.read`، وإنشاء المسودة صلاحية `inbox.reply`. يعرض مركز البوت حالة القناتين وCallback URL والحالة الأخيرة ولا يعرض token. يعرض Inbox صورة مصغرة قابلة للفتح، حالة التحليل، الوصف والمرشحين، وزر إعادة التحليل للمخول، مع توضيح أن النتيجة اقتراح وليست إثبات هوية.

## الاختبارات الإلزامية

تغطي الاختبارات challenge الصحيح والخاطئ، توقيع HMAC الصحيح والخاطئ، منع التكرار، رفض حساب غير مربوط، عزل المتجر، تطبيع WhatsApp وInstagram، تنزيل JPEG/PNG وحد الحجم والنوع، فشل التنزيل، تخزين S3 بمحاكٍ، JSON الرؤية، عدم تسرب التكلفة، مطابقة المنتجات داخل المتجر فقط، حزمة BotFacts، والتحويل البشري عند الثقة المنخفضة. لا تستخدم الاختبارات Meta أو نموذجاً حياً.

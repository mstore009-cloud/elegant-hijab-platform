بناءً على العرض الهندسي المقدم من AWS SaaS Factory، إليك استخراج تفصيلي للإرشادات العملية لبناء بنية SaaS متعددة المستأجرين (Multi-tenant) باستخدام تقنيات Serverless، مقسمة حسب الفئات المطلوبة:

### 1. تعريف المستأجر (Tenant Definition)
يجب تقسيم بنية النظام منذ البداية إلى قسمين رئيسيين: **مستوى التطبيق (Application Plane)** وهو الخدمة الفعلية المقدمة، و**مستوى التحكم (Control Plane)** وهو المسؤول عن إدارة المستأجرين.
*   **مستويات المستأجرين (Tiering):** يجب تصنيف المستأجرين إلى فئات (مثل: أساسي، قياسي، بلاتيني) لأن هذا يحدد البنية التحتية الخاصة بهم.
*   **عملية التسجيل (Onboarding):** تبدأ من تطبيق التسجيل (أو لوحة تحكم الإدارة لـ B2B)، وتستدعي خدمة "تسجيل المستأجر" (Registration Microservice) التي تقوم بإنشاء معرّف فريد للمستأجر (`tenant_id`)، وإعداد هويته، وحفظ بياناته في جدول إدارة المستأجرين (Tenant Management Table).
*   **اقتباس:** *"المستأجرون في الفئة الأساسية والقياسية يتم نشرهم في نموذج مجمّع (Pool model)... بينما عندما ننشئ مستأجراً جديداً كفئة بلاتينية، فإننا نقوم فعلياً بنشر موارد منفصلة لهؤلاء المستأجرين."*

### 2. عزل البيانات (Data Isolation)
يتم تحديد استراتيجية العزل بناءً على فئة المستأجر، وتعتمد على نموذجين:
*   **نموذج العزل (Silo Model):** يخصص موارد بنية تحتية مستقلة (مثل جدول DynamoDB منفصل) لكل مستأجر. يُستخدم للفئات العليا لتلبية متطلبات الامتثال (Compliance) واتفاقيات مستوى الخدمة (SLAs) الصارمة.
*   **نموذج التجميع (Pool Model):** يتشارك المستأجرون نفس الموارد (جدول DynamoDB واحد للجميع) لتقليل التكلفة.
*   **فرض العزل في نموذج التجميع (المخاطر والحلول):**
    *   **الخطر:** الاعتماد على المطورين لكتابة كود برمجي (مثل `WHERE tenant_id = X`) لعزل البيانات هو أمر *"معرض للكثير من الأخطاء"* ويضعف الأمان.
    *   **الحل (السياسات الديناميكية):** استخدم AWS STS (Security Token Service) لتوليد صلاحيات مؤقتة (Short-lived credentials) لدالة Lambda في وقت التشغيل (Runtime).
    *   **اقتباس:** *"هذه السياسة تسمح لك بإجراء هذه الإجراءات الخمسة على جدول DynamoDB... ولكن يُسمح بالإجراء فقط على تلك الصفوف التي تبدأ بالمفتاح الأساسي لمعرّف المستأجر (tenant ID)."*

### 3. الهوية والصلاحيات (Identity & Permissions)
*   **إدارة المستخدمين:** استخدم Amazon Cognito. يمكن إنشاء "تجمع مستخدمين" (User Pool) منفصل لمستأجري الـ Silo، وتجمع مشترك لمستأجري الـ Pool.
*   **السمات المخصصة (Custom Attributes):** يجب حقن معرّف المستأجر (`tenant_id`) ودور المستخدم (`user_role`) كسمات مخصصة داخل رمز التوثيق (JWT Token) أثناء التسجيل.
*   **التحكم في الوصول (Authorization):** استخدم Lambda Authorizer مع API Gateway لاعتراض الطلبات. يقوم الـ Authorizer بقراءة دور المستخدم من الـ JWT وبناء سياسة (IAM Policy) تسمح أو تمنع الوصول لنقاط نهاية محددة (مثلاً: منع طلبات POST/PUT للمستخدمين بصلاحية القراءة فقط).
*   **اقتباس:** *"هذه هي طريقتنا لإخبار مزود الهوية (IDP) بأي مستأجر ينتمي إليه هذا المستخدم. والطريقة التي نفرض بها ذلك هي باستخدام السمات المخصصة."*

### 4. المراقبة والتدقيق (Monitoring & Auditing)
*   **الحد من الاستهلاك (Throttling / Noisy Neighbor):** لحماية النظام من استهلاك مستأجر واحد لموارد النظام بالكامل، يجب إنشاء مفتاح واجهة برمجة تطبيقات (API Key) لكل مستأجر عند التسجيل وربطه بخطة استخدام (Usage Plan) في API Gateway.
*   **اقتباس:** *"إذا كان لديك مستخدم يحاول إساءة استخدام النظام... فإن هذا الاستغلال يقتصر على ذلك المستأجر فقط لأنك الآن تقول: مهلاً، هذا المستأجر يمكنه الوصول إلى الـ API 100 مرة فقط في اليوم."*
*   **المركزية:** استخدم Lambda Layers لإنشاء مكتبات قابلة لإعادة الاستخدام (Reusable libraries) لجمع السجلات (Logging) والمقاييس (Metrics) وتطبيقها على جميع دوال Lambda لضمان توحيد المراقبة.

### 5. التكاملات والأحداث (Integrations & Events)
*   **توجيه المستأجرين (Tenant Routing):** للوصول إلى النظام، يمكن استخدام نطاقات فرعية (Subdomains) (مثل: `tenant1.saas.com`)، أو توجيه المستخدمين بعد تسجيل الدخول عبر استدعاء خدمة "إدارة المستأجرين" لمعرفة رابط API Gateway و Cognito User Pool الخاصين بكل مستأجر.
*   **نشر التحديثات (CI/CD):**
    *   **الخطر:** *"في بيئة SaaS، لا تقوم بعمليات نشر فردية (One-off deployments). هذا نمط مضاد (Anti-pattern)."*
    *   **الحل:** يجب أن يمر خط أنابيب النشر (Pipeline) عبر جدول المستأجرين لتحديث جميع الموارد (المجمعة والمعزولة) بشكل متسق، مع استخدام النشر التدريجي (Canary deployments) لتحويل حركة المرور بأمان للنسخ الجديدة.
*   **تكاملات الخدمات المباشرة (مثل AppSync/Step Functions):**
    *   **الخطر:** التكامل المباشر يتجاوز Lambda Authorizer، مما يجعل تطبيق السياسات الديناميكية (STS) لنموذج الـ Pool صعباً.
    *   **الحل:** يُنصح بوضع دالة Lambda كوسيط (Intermediary) لفرض الأمان، حتى للعمليات البسيطة (CRUD)، لضمان تطبيق الصلاحيات المؤقتة.

### 6. ما الذي يجب بناؤه مبكراً مقابل ما يؤجل (Build Early vs. Defer)
*   **يُبنى مبكراً (Build Early):**
    *   **مستوى التحكم (Control Plane):** يجب بناء خدمات التسجيل، وإدارة المستأجرين، وإدارة المستخدمين قبل أو بالتوازي مع التطبيق الفعلي، لأنها الأساس الذي يُبنى عليه عزل البيانات.
    *   **السياسات الديناميكية (Dynamic Policies):** يجب برمجة دوال Lambda لاستخدام AWS STS للحصول على صلاحيات محددة بنطاق المستأجر (Tenant-scoped) منذ اليوم الأول، وعدم تأجيل ذلك أو الاعتماد على أكواد التطبيق (Application-level checks).
    *   **خطوط النشر (CI/CD):** أتمتة النشر عبر بيئات المستأجرين المتعددة يجب أن تكون جاهزة مبكراً لضمان اتساق الإصدارات.
*   **يُؤجل (Defer):**
    *   **التخزين المؤقت للصلاحيات (Caching Authorizer):** عملية استدعاء STS تولد تأخيراً (Latency) يقدر بحوالي 200 مللي ثانية. يمكن تأجيل تحسين هذا التأخير؛ لاحقاً يمكن تفعيل ميزة التخزين المؤقت (Caching) المدمجة في Lambda Authorizer (مثلاً لمدة 5 دقائق) لتجنب استدعاء STS مع كل طلب.
    *   **اقتباس:** *"يوفر Lambda Authorizer ميزة التخزين المؤقت... يمكنك تخزين النتيجة مؤقتاً... وبذلك لن تضطر بالضرورة لإجراء هذا الاستدعاء في كل مرة."*
# نتيجة معاينة شجرة OneDrive الحية

**التاريخ:** 2026-09-05

تمت قراءة شجرة مصدر OneDrive للمتجر التشغيلي عبر حساب التفويض المخزن مشفراً. لم تُنزَّل وسائط، ولم تُنشأ أو تُعدَّل منتجات، ولم تُحفظ روابط مشاركة أو رموز وصول في هذا الملف.

| المؤشر | النتيجة |
|---|---:|
| التصنيفات المقروءة | 7 |
| مجلدات المنتجات المطابقة لبنية المعلومات + الوسائط | 5 |
| مجلدات تحتاج مراجعة | 7 |
| إجمالي المجلدات المقروءة | 19 |
| حد العمق | لم يُبلَغ |

تظهر شجرة `Catalog` تصنيفاً رئيسياً باسم **حجابات الاميرة** يضم خمسة مجلدات منتجات مطابقة: `h02` و`h03` و`h04` و`H12 test` و`HJB-TEST-001`. كما تظهر شجرة **ربطات → قطن سادة → قياس متر**، لكن مجلدات مثل `1` إلى `7` تحتوي وسائط من دون ملف `product.txt` أو `product.docx`، ولذلك صُنّفت «تحتاج مراجعة» ولم تعامل كمنتجات.

> قاعدة الاستيراد المعتمدة: لا يُعامل المجلد كمنتج إلا إذا احتوى ملف معلومات `product.txt` أو `product.docx` وصورة أو فيديو واحداً على الأقل. المجلد الذي يحوي مجلدات فرعية يعامل تصنيفاً، أما المجلد غير المطابق فيبقى للمراجعة ولا ينشئ منتجاً تلقائياً.

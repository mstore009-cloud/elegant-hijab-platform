# دراسة Channel-Vision-Bot-A

**تاريخ الدراسة:** 2026-08-27  
**قرار التنفيذ السابق المعتمد:** الربط المباشر الرسمي مع Meta، لا وسيط خارجي، مع بقاء الإرسال التلقائي معطلاً حتى التفويض والاختبار.

## ما تثبته الوثائق الرسمية

تدعم WhatsApp Business Platform استقبال الرسائل الواردة وحالات الرسائل عبر webhook، وتتطلب صلاحية `whatsapp_business_messaging` لاستقبال `messages`. توقع Meta الحمولة في `X-Hub-Signature-256` باستخدام HMAC-SHA256 ومفتاح App Secret، وقد تعيد تسليم الحدث عدة مرات حتى سبعة أيام، لذلك يجب التحقق من التوقيع ومنع التكرار قبل الكتابة.[1][2]

تتضمن رسائل الوسائط الواردة معرف Media ID. رابط التنزيل قصير الصلاحية ويحتاج Access Token، لذلك يجب تنزيل الصورة فوراً إلى تخزين المنصة وعدم الاحتفاظ برابط Meta كمصدر دائم. صور WhatsApp المدعومة للتحليل في هذه الدفعة هي JPEG وPNG حتى 5MB.[3]

يدعم Instagram Messaging webhook رسائل النص والوسائط والمشاركات والردود على القصص للحساب الاحترافي. يتطلب حساباً احترافياً وتطبيقاً منشوراً وصلاحيات الرسائل المناسبة، وقد يتطلب App Review للوصول إلى مستخدمين خارج أدوار التطبيق. لا يدعم GIF/sticker وبعض الوسائط المختفية، كما أن المحادثة تبدأ برسالة من العميل وتخضع نافذة الرد المعتادة إلى 24 ساعة.[4][5]

## خيارات التنفيذ

| النهج | المفاضلات | التكلفة | تعقيد الإعداد |
|---|---|---|---|
| الربط المباشر الرسمي مع Meta | تحكم كامل في البيانات والتوقيع والعزل، من دون وسيط؛ يحتاج تطبيق Meta وصلاحيات ومراجعة وتفويض حسابات | رسوم Meta الفعلية واستهلاك نموذج الرؤية فقط؛ لا رسم وسيط | مرتفع نسبياً |
| مزود وسيط لإدارة القنوات | إعداد حسابات أسهل ودعم تشغيلي جاهز؛ يضيف مورداً خارجياً وطبقة أسعار واعتماداً على مزود | رسوم Meta إضافة إلى اشتراك/هامش المزود | متوسط |

الخيار الأول هو القرار السابق المعتمد للمشروع. سيتم بناء نقطة webhook على خادم المنصة المنشور لأن الاستقبال حدث HTTP مباشر ولا يحتاج عملية دائمة أو فحصاً دورياً. لن تفتح الدفعة أي اتصال أو تنفق رسائل قبل إضافة الأسرار وتفويض الحساب في مرحلة مستقلة.

## معمارية الدفعة

| الطبقة | المسؤولية |
|---|---|
| حساب القناة | يربط متجر المنصة بمعرف WABA/رقم WhatsApp أو Instagram Professional ID، مع حالة `disconnected/testing/connected/disabled` ومنع تكرار المعرف عبر المتاجر |
| webhook | GET للتحقق من verify token، وPOST بقراءة raw body والتحقق من `X-Hub-Signature-256` ثم إقرار سريع |
| سجل الأحداث | يحفظ hash ومعرف الحدث/الرسالة والحالة وعدد المحاولات، ويمنع أثر التكرار مع إبقاء دليل قابل للتدقيق |
| تطبيع الرسالة | يحول payload الخاص بكل قناة إلى محادثة ورسالة Inbox ضمن `storeId` وحساب القناة الصحيح |
| وسائط الرسالة | ينزل الصورة الموثقة إلى S3 ويحفظ المفتاح والنوع والحجم وSHA-256؛ لا يخزن bytes في قاعدة البيانات |
| تحليل الصورة | يستخدم `gemini-3-flash-preview` المتاح في الكتالوج الحي مع JSON schema، وحد ثقة وتحويل بشري عند الشك |
| مطابقة المنتج | يصف نوع المنتج واللون والنقش والنص الظاهر، ثم يقترح منتجات نشطة من المتجر فقط؛ الاقتراح ليس إثبات هوية ويظهر للموظف مع الثقة |
| Bot-H1/H2 | يضيف تحليل الصورة والمطابقات إلى `factsSnapshot` دون تكلفة أو هامش، ثم يولد مسودة قابلة للمراجعة وفق المسار الحالي |
| واجهة الموظف | تعرض الصورة والتحليل والمطابقات ومسودة الرد داخل Inbox، وتعرض صحة الاتصال والأخطاء في مركز البوت |

## ضوابط الأمان والخصوصية

يرفض webhook غير الموقع قبل أي كتابة. يمنع تكرار `externalMessageId` وhash الحدث. لا تُعالج إلا `image/jpeg` و`image/png` ضمن الحجم المحدد، مع مهلة تنزيل وحد أقصى للحجم والتحقق من النوع الفعلي. لا يرسل نموذج الرؤية التكلفة أو الهامش أو بيانات عميل آخر، ولا يغير منتجاً أو مخزوناً أو طلباً. تحفظ نتيجة التحليل كسجل غير تنفيذي ويظل الرد مسودة بشرية.

لا نخزن Access Tokens في الجداول أو السجلات أو payload الخام؛ تحفظ عبر أسرار البيئة فقط. بيانات الحساب غير السرية مثل Phone Number ID وInstagram Account ID يمكن ربطها بالمتجر، أما App Secret وVerify Token وAccess Tokens فتدخل في مرحلة التفويض الآمن.

## حدود المرحلة قبل التفويض

يمكن إنجاز المخطط والخدمة وwebhook والتحليل والواجهة والاختبارات باستخدام fixtures موقعة ومحاكي تنزيل للوسائط. يبقى استقبال Meta الحي معلقاً حتى يقدم المستخدم حساب Meta Business وتطبيقه أو يسمح بإنشائه، ويمنح الصلاحيات المطلوبة ويزود الأسرار عبر لوحة آمنة. لن يتم تفعيل `auto_reply` أو إرسال رسالة خارجية ضمن هذه الوحدة.

## المراجع

[1]: https://developers.facebook.com/documentation/business-messaging/whatsapp/webhooks/overview "WhatsApp Webhooks Overview"
[2]: https://developers.facebook.com/documentation/business-messaging/whatsapp/webhooks/create-webhook-endpoint/ "Create a WhatsApp Webhook Endpoint"
[3]: https://developers.facebook.com/documentation/business-messaging/whatsapp/business-phone-numbers/media "WhatsApp Media API"
[4]: https://developers.facebook.com/documentation/business-messaging/instagram-messaging/webhooks "Instagram Messaging Webhooks"
[5]: https://developers.facebook.com/documentation/instagram-platform/instagram-api-with-instagram-login/messaging-api "Instagram Messaging API"

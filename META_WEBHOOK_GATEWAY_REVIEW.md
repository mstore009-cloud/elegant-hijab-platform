# مراجعة Unified Meta Webhook Gateway

**التاريخ:** 2026-08-28  
**النسخة الأساس:** v0.89 (`735e1858`)

## النطاق المنفذ

تستقبل البوابة payload موقّعاً واحداً من Meta وتطبع رسائل WhatsApp وMessenger وInstagram، وحالات تسليم WhatsApp وMessenger، والتعليقات والـmentions وLead Ads وحالات النشر إلى أحداث داخلية موحدة. يحدد المتجر من حساب القناة أو أصل Meta المختار ولا يقبل `storeId` من الطلب الخارجي.

يحجز كل حدث مرة واحدة، ويخزن نسخة مطبعة غير سرية لإعادة المعالجة، ويطبق تدرج retry حتى خمس محاولات ثم ينقله إلى dead-letter. تعرض صفحة `/meta-connections` عدد الأحداث المعلقة وdead-letter وتسمح للمخول بإعادتها يدوياً. لا تُحفظ روابط وسائط Meta المؤقتة في سجل retry.

## Heartbeat

أنشئت مهمة مشروع دائمة باسم `meta-webhook-retry` وربطت بـ`task_uid` محفوظ في `meta_webhook_retry_settings`. اختُبرت أولاً كل دقيقة، ونفذت endpoint المنشور بحالة HTTP 200 والنتيجة `attempted: 0, processed: 0, deadLetters: 0`. بعد النجاح أُعيد التكرار إلى كل خمس دقائق: `0 */5 * * * *`.

| التحقق | النتيجة |
|---|---|
| فحص الأنواع | ناجح |
| الاختبارات الكاملة | 57 ملفاً و118 اختباراً ناجحاً |
| Messenger | تطبيع رسالة واردة ومنع تكرارها وإنشاء رسالة Inbox واحدة |
| حالات التسليم | تحديث رسالة صادرة داخل المتجر إلى delivered/read/failed |
| Retry وdead-letter | اختبار انتقال الحدث وإعادته يدوياً وعزل المتجر |
| Heartbeat الحي | نجاح HTTP 200 على النطاق المنشور، ثم ضبطه كل خمس دقائق |

## الحدود

تُحفظ التعليقات والـmentions وLead Ads وحالات النشر الآن كأحداث موحدة فقط؛ تعالجها وحداتها المتخصصة في المراحل اللاحقة. لا يوجد إرسال يدوي أو رد مباشر أو نشر أو إنفاق، ولا توجد حسابات Meta مفوضة بعد.


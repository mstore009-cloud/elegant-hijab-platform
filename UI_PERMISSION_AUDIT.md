# تقرير تدقيق المتجر والصلاحيات وتعدد المتاجر

**المشروع:** منصة عالم الحجابات الأنيقة

**نطاق التدقيق:** مطابقة ما هو موجود في قاعدة البيانات والخادم مع ما يظهر فعلياً في الواجهة التشغيلية، مع التركيز على المتجر الحالي، تعدد المتاجر، صلاحيات الموظفين، وربط Meta.

**الحالة:** تدقيق فقط. لم يُنفذ أي تعديل برمجي على سلوك المنصة.

## الخلاصة التنفيذية

ملاحظتك صحيحة. المنصة الحالية لا تحتوي على نظام تعدد متاجر مكتمل من منظور المستخدم التشغيلي. الموجود حالياً هو **متجر افتراضي واحد قابل للتشغيل** باسم «عالم الحجابات الأنيقة»، مع نواة بيانات صُممت بحيث تحمل معظم السجلات `storeId` وتسمح بالعزل بين المتاجر مستقبلاً. هذه النواة ليست مساوية لوجود منصة متعددة المتاجر قابلة للإدارة.

الواجهة الحالية لا تعرض قائمة متاجر، ولا تتيح إنشاء متجر أو تعديل هويته أو تعليقه أو اختيار متجر آخر أو التبديل بين المتاجر. كما أن جميع الوحدات، بما فيها الصلاحيات وإعدادات المتجر وMeta وInbox وCRM والطلبات، تعمل على **المتجر التشغيلي الذي يحدده الخادم تلقائياً**. لذلك كان وصف المنصة سابقاً بأنها متعددة المتاجر أوسع من الواقع التشغيلي الحالي، ويجب تصحيحه.

## ما تم التحقق منه في الواجهة

| الجزء | ما يظهر للمستخدم حالياً | الاستنتاج |
|---|---|---|
| الشريط الجانبي والرأس | وحدات المنتجات والطلبات والعملاء وInbox والبوتات والتسويق والتحليلات والصلاحيات وإعدادات المتجر وMeta | لا توجد وحدة «إدارة المتاجر» ولا محدد متجر في الرأس أو القائمة |
| النظرة التشغيلية `/` | اسم المتجر التشغيلي الحالي، وعدد العمليات والصلاحيات والوصول | عرض معلوماتي لمتجر واحد، دون تغيير السياق |
| الصلاحيات `/permissions` | كتالوج عمليات مفصل، قائمة حسابات، ربط الحساب بالمتجر الحالي، تفعيل/إيقاف الحساب، وسجل تدقيق | إدارة موظفين داخل متجر السياق الحالي، وليست إدارة عضوية عبر عدة متاجر |
| إعدادات المتجر `/settings/store` | اللغة، العملة، أجرة التوصيل، التوصيل المجاني، والقسائم | إعدادات تشغيلية للمتجر الحالي فقط؛ لا توجد هوية متجر أو قائمة متاجر أو إنشاء متجر |
| ربط Meta `/meta-connections` | اتصال موحد، أصول Meta، القدرات، Webhook، الصحة، المزامنة، والإبطال | كل العرض والتنفيذ مرتبطان بالمتجر التشغيلي الحالي؛ لا يوجد اختيار متجر مستهدف |
| واجهة المتجر `/store` | واجهة عامة مرتبطة بالـslug الافتراضي `elegant-hijab` | متجر عام واحد في الإصدار الحالي، وليس واجهات متاجر متعددة قابلة للإدارة |

## ما تم التحقق منه في الخادم وقاعدة البيانات

يعرّف `server/stores/db.ts` المتجر الافتراضي باسم `elegant-hijab`، ويذكر صراحةً أن الإصدار الأول يعرض متجراً واحداً لكل حساب. الدالة `getOperationalStoreContext` تبحث عن ملف الموظف للمستخدم، ثم تعيد متجراً واحداً فقط. وإذا كان المستخدم مدير منصة ولا يملك ملف موظف، تُعيد المتجر الافتراضي.

أما `server/routers/stores.ts` فلا يصدّر سوى الإجراء `stores.current`. لا توجد إجراءات إنتاجية لعرض قائمة المتاجر أو إنشاء متجر أو تعديل بياناته أو تعليقه أو اختيار متجر أو التبديل بين المتاجر. هذا يفسر مباشرةً لماذا لا يجد المستخدم هذه الخيارات في الواجهة.

في قاعدة البيانات يوجد جدول `stores` مع الحقول `name` و`slug` و`status` و`primaryOwnerUserId`، كما تحمل جداول المنتجات والطلبات والعملاء والمحادثات وعمليات Meta حقلاً `storeId`. هذا يثبت وجود أساس جيد للعزل، لكنه لا يضيف وحده واجهة الإدارة أو دورة حياة المتجر.

| طبقة النظام | الموجود | غير الموجود |
|---|---|---|
| قاعدة البيانات | جدول متاجر، مالك أساسي، slug، حالة، وعزل `storeId` | لا يكفي وحده لإدارة المتاجر من الواجهة |
| سياق الطلب | `operationalStore` واحد لكل طلب | لا يوجد `selectedStoreId` يختاره المستخدم |
| API المتاجر | `stores.current` فقط | `list`, `create`, `update`, `switch`, `suspend`, `archive` |
| عضوية الموظف | ملف موظف مربوط بمتجر | `employeeProfiles.userId` فريد؛ المستخدم لا يملك حالياً عضوية في أكثر من متجر |
| الصلاحيات | صلاحيات ذرية كثيرة وحارس خادمي | لا يوجد نطاق متجر يختاره المدير من الواجهة |
| واجهة Meta | مسار مالك التطبيق ومسار عميل خارجي | لا يوجد محدد متجر أو إدارة اتصالات لعدة متاجر |

## الصلاحيات الفعلية

كتالوج الصلاحيات موجود ومفصل، ويشمل صلاحيات المنتجات والطلبات والعملاء وInbox والبوت والمحتوى والتسويق والتحليلات والولاء والموظفين والإعدادات والبيانات المالية الحساسة. هذا الجزء منفذ فعلياً داخل نطاق المتجر الحالي، وليس مفقوداً بالكامل.

لكن صفحة الصلاحيات نفسها تقول للمستخدم إنه يختار حساباً «لربطه بالمتجر الحالي». عملية الحفظ لا تستقبل `storeId` من الواجهة؛ الخادم يأخذه من `ctx.operationalStore.id`. كما أن `employeeProfiles.userId` فريد، وتبحث دالة الحفظ عن ملف المستخدم بالـ`userId` فقط وترفض تعديل ملف مرتبط بمتجر آخر. النتيجة هي أن النظام الحالي يدعم **حساباً واحداً في نطاق متجر واحد**، لا موظفاً مشتركاً بين عدة متاجر.

## سبب مشكلة حساب المراجعة وMeta

المشكلة التي ظهرت مع حساب المراجعة ليست بالضرورة خطأ في منح الصلاحيات. هناك نوعان مختلفان من الصلاحيات:

| النوع | وظيفته |
|---|---|
| صلاحيات مستخدم المنصة | تحديد ما يستطيع الحساب فعله داخل المتجر التشغيلي، مثل قراءة Inbox أو إدارة الإعدادات |
| صلاحيات Meta | تفويض Facebook/Meta والوصول إلى Business Portfolio والأصول التابعة لها |

في واجهة Meta، الوصول إلى overview يتطلب `settings.manage` ضمن المتجر التشغيلي. لكن بدء مسار ربط مالك التطبيق `beginUnifiedAuthorization` محمي بحارس إضافي لا يسمح به إلا للمستخدم ذي `role=admin`. لذلك فإن الحساب الجديد قد يستطيع رؤية بعض إعدادات المتجر إذا رُبط بالمتجر ومنح الصلاحية المناسبة، لكنه لا يستطيع استعمال مسار ربط مالك التطبيق المباشر.

كذلك، مسار `beginExternalBusinessAuthorization` موجود، لكنه يعمل على المتجر التشغيلي الحالي فقط، ويتطلب إعدادات القالب وصلاحية `settings.manage`. لا توجد حالياً واجهة تختار منها أي متجر سيستقبل الاتصال الجديد. لذلك لا ينبغي وصف الحساب الجديد بأنه يستطيع «ربط Meta لأي متجر»؛ هذا غير موجود في الواجهة ولا في نموذج السياق الحالي.

## حالة البيانات الفعلية

أظهر استعلام قراءة لجدول `stores` وجود متجر الإنتاج `عالم الحجابات الأنيقة` بالـslug `elegant-hijab`، إضافة إلى عدة سجلات متاجر اختبار أنشأتها اختبارات التكامل أو مراحل التطوير. وجود هذه السجلات لا يعني أن المستخدم يستطيع الوصول إليها أو إدارتها؛ فلا توجد واجهة قائمة أو تبديل أو ربط عضوية تعرضها.

كما ظهر ملف موظف فعلي مرتبط بالمتجر الإنتاجي. هذا يتفق مع ما تعرضه الواجهة: موظفون وصلاحيات ضمن المتجر الحالي، لا إدارة متعددة للمتاجر.

## التصنيف النهائي

| الحالة | العناصر |
|---|---|
| منفذ وظاهر | متجر افتراضي واحد، إعدادات تشغيلية، صلاحيات ذرية، عزل بيانات حسب `storeId`، اتصال Meta موحد للمتجر الحالي، Inbox وCRM وطلبات مرتبطة بالسياق |
| منفذ في الخادم فقط أو كبنية داخلية | وجود جدول متاجر وسجلات اختبار متعددة، امتلاك المتاجر لاسم وslug وحالة ومالك، ومسارات Meta الخارجية القابلة للتوسع |
| غير موجود تشغيلياً | إنشاء متجر، قائمة المتاجر، تبديل المتجر، إدارة مالك المتجر، إدارة حالة المتجر، عضوية موظف في عدة متاجر، اختيار المتجر عند ربط Meta |
| يحتاج معالجة مستقلة | خطأ runtime سابق في سجل المتصفح بسبب استيراد `inspectInstagramWebhookSubscription` باسم لا يطابق التصدير في ملف OAuth؛ TypeScript يمر، لكن يجب إغلاق هذا الخلل قبل اعتبار مركز Meta مستقراً بالكامل |

## القرار المقترح قبل البرمجة

يجب اعتماد الوصف التالي للنسخة الحالية: **«منصة تشغيل لمتجر افتراضي واحد، مبنية على نواة بيانات معزولة وقابلة للتوسع إلى تعدد المتاجر»**. لا ينبغي استخدام عبارة «منصة متعددة المتاجر» في تعليمات المستخدم أو تعليمات مراجع Meta إلا بعد تنفيذ طبقة الإدارة والواجهة اللازمة.

أما الإضافة الصحيحة لاحقاً، فيجب أن تكون وحدة مستقلة باسم **إدارة المتاجر** تشمل قائمة المتاجر، إنشاء متجر، بيانات الهوية، الحالة والمالك، ربط الموظفين بعدة متاجر عند الحاجة، محدداً عالمياً للسياق، وقواعد واضحة لتبديل المتجر. بعدها تُراجع كل وحدة تشغيلية—بما فيها Meta وInbox وCRM والطلبات والإعدادات—لتؤكد أنها تعرض اسم المتجر المختار وتستخدمه في كل قراءة وكتابة.

هذا التدقيق لم ينفذ تلك الإضافة، ولم يغيّر سلوك الحساب أو الصلاحيات. لقد وثّق فقط الفارق بين النواة الموجودة وبين ما هو متاح فعلياً للمستخدم.

## المراجع الداخلية

1. `client/src/App.tsx` — مسارات التطبيق وفصل الواجهة العامة عن لوحة التشغيل.
2. `client/src/components/DashboardLayout.tsx` — القائمة الجانبية والرأس، ولا يحتويان على محدد متجر.
3. `client/src/pages/AccessControl.tsx` — واجهة الصلاحيات داخل المتجر الحالي.
4. `client/src/pages/StoreSettings.tsx` — إعدادات اللغة والعملة والتوصيل والقسائم فقط.
5. `client/src/pages/MetaConnections.tsx` — صلاحية `settings.manage` وعرض اتصال Meta ضمن المتجر الحالي.
6. `server/stores/db.ts` — حل المتجر التشغيلي والمتجر الافتراضي.
7. `server/routers/stores.ts` — الإجراء الوحيد `stores.current`.
8. `server/access/db.ts` — قائمة الموظفين وحفظ ملف موظف واحد لكل مستخدم.
9. `server/access/permissions.ts` — كتالوج الصلاحيات الذرية.
10. `server/routers/metaConnections.ts` — حراس ربط Meta ومسارات المالك والعميل الخارجي.
11. `drizzle/schema.ts` — جداول `stores` و`employee_profiles` وحقول العزل `storeId`.
